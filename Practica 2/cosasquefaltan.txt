EJERCICIO 1
1. Hacer que cuando se modifique los intereses de un contacto se borre el usuario de la tabla destinatarios y de intereses_contacto. Revisar si esta hecho
2. En modificarAnuncios tener cuidado, hay que modificar varias tablas a veces creo
3. Mirar en todos los statements que tengan el status y su consecuente if por si dan fallos.
4. Modificar las fechas en controldeerrores y donde esten como dd/mm/yyyy. En concreteFactory de ANuncioFlash DEBERIA PONER HORAS-MINUTOS-SEGUNDOS
5. En actualizarContacto...()creo que se deberia hacer el control de errores, en el caso de fecha con try catch creo que es suficiente.
6. En mostrarAnuncios del TABLON hacer una funcion en anunciosDAO para actualizar el estado de los anuncios flash(Pasarlos a publicado y archivado)
7. En lo de INSERTAR_INTERES_CONTACTO a ver porque deja insertarlo si no hay correo de antes. Deberia de hacerse despues de insertar el correo
8. En la practica 2 actualizar las funciones de buscarContacto()
9. Actualizar metodos toString de cada anuncio.

Para guardarAnuncio en individualizado o funcion aparte mejor:

ArrayList<Contacto> destinatarios=new ArrayList<Contacto>();
		boolean condicion=true;
		String linea=new String();
		
		while(condicion) {
			
			try {
				
				System.out.print("Introduzca el Email del destinatario: ");
			
				linea=sc.nextLine();
				
				for(Contacto d: contactosDAO.getContactos()) {
					if(d.getEmail().equals(linea.toLowerCase())) {
						destinatarios.add(d);
						
						break;
					}
				}
				System.out.println("Quieres añadir otro destinatario: 1. Si 2.No");
				as=sc.nextInt();
				sc.nextLine();
				
				if(as!=1) {
					condicion=false;
				}
				
			} catch (NoSuchElementException w) {
				
                System.out.println("Debes insertar un numero");
	             
               as=sc.nextInt();
               sc.nextLine();

            }
     }
